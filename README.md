# A32 connected apartment model

This Three.js model uses the two supplied floor plans. One model unit equals one metre.

Run `npm install`, then `npm run dev`. Open the local address printed by the server.

The published site is at https://alex-alecu.github.io/intre-lacuri-residence/. GitHub Actions checks and publishes the site when a change is pushed to `main`. Run `npm run build:pages` to make the same static output locally. The output is in `dist/client`. It includes the model and extracted dimensions. It excludes the original PDF and plan image. File links use the repository path.

The Pages workflow runs on Linux. The installed vinext version can stop with a libuv shutdown error after static export on Windows with Node.js 24. The workflow requires a successful build and file checks before it can publish.

The 3D overview has cut walls. Drag to rotate. Use the wheel to zoom. Select a room to move the camera.

Use the three controls above the model to compare the layouts. **01 Living + birou** keeps the original living room and both offices. **02 Dormitor + dressing** adds a master bedroom in the west living room and a dressing area in the south-west office. **03 Living + dining** keeps the west bedroom and dressing room, then changes the east apartment to a living and dining area. Version 3 opens first. The `layout=original`, `layout=suite`, and `layout=social` URL parameters keep the selected version after a page reload. Switching keeps the camera, view mode, light setting, and furniture setting. It pauses walking and moves the player to clear floor if necessary.

The new enclosure has a 15 cm east wall and a short north return. The east face aligns with the utility wall. A 1 m door opens into the bedroom. The north common passage remains 1.075 m wide. Versions 2 and 3 move the children to the north-west corner room beside the west master bedroom. The former child room becomes a gaming and desk room. Version 2 has three bedrooms and one office. Version 3 has two bedrooms and one office.

The west bedroom has a 2.00 × 2.00 m bed, two bedside tables, a small desk beside the bed, and a 2.20 m wide cabinet for folded clothes in place of the TV. The desk is 1.20 × 0.68 m and faces the west window. The new cabinet is 36 cm deep. Both versions 2 and 3 include it. The conservative model clearance is 0.81 m on the east side of the bed and 0.75 m at the foot (0.80 m between the visible meshes). The dressing room has two closed walnut wardrobe runs, 2.20 m and 1.60 m long, with sliding doors, a full-length mirror, and a seat. The west window, column, and service shaft remain clear. A walnut filler closes the small wardrobe corner joint. The proposed enclosed floor area is 20.94 m², including 5.69 m² for the open dressing zone. These are model areas, not source room labels or site measurements.

Version 3 moves the six-seat table into the former east bedroom. The shared wall is retained to 1.35 m, with a stone cap at 1.40 m. A separate kitchen worktop stays at 0.95 m. This keeps the requested half-height wall and a lower surface for food preparation. The left support and service shaft stay at full height. Walnut covers the support faces beside the island. A 1.05 m passage opens at the east end. Upper cabinets above the island are removed. These are model proposals. A structural engineer must check the support dimensions and both wall changes before site work.

The straight 2.60 m sofa faces a TV that is 1.60 m wide. The kitchen aisle behind the sofa is about 1.04 m. A compact 0.85 × 0.40 m coffee table leaves space in front. The straight sofa replaces the corner sofa in version 3 to keep this space clear. In version 1, the child wardrobe faces east beside the closed toy cabinet. The original office cabinet faces west and clears the desk.

The child’s room in versions 2 and 3 follows the supplied loft-room photo. A cream and light-oak loft has a 0.90 × 1.90 m mattress, a 1.38 m deck, stairs, and a white slide. A reading tent, warm string lights, and low toy shelves fit below the bed. A round table, two padded stools, and soft rugs form the play area. The 2.75 m south wardrobe stays in place. The north balcony door opens outward. Both balcony paths stay clear. About 0.81 m remains between the loft and wardrobe. The loft is a furniture design proposal, not a construction drawing. Version 1 keeps the previous house-bed room for comparison.

The version 2 office has a 1.30 m desk beside the TV on the west wall. The desk is at the north end, near the window, and faces west. The TV is at the south end of the same wall. The 2.40 m sofa is near the room centre, facing the TV, with its back towards the east wardrobe. The wall cabinets are above the TV. Version 3 keeps the west desk and south TV positions. Both office versions have a 2.96 m wardrobe with sliding doors, a small storage table, and a 1.05 m media cabinet. The active TV screen has a 1.80 m diagonal and a 16:9 ratio. It is about 1.569 m wide and 0.883 m high. These dimensions exclude the frame.

Storage has closed fronts, except for the low toy shelves below the new loft, as shown in the supplied photo. The layout checks sample the replacement cabinet fronts at four heights and four positions to check that they cover the storage openings.

The family-room checks use rendered mesh bounds to check furniture pairs, the pulled-out office chair, and 49 door positions from closed to open. They also check the room routes on a 0.04 m grid with a 0.19 m person radius. All room furniture stays solid for these route checks. Thus, the viewer rule that ignores furniture near walls cannot hide a blocked passage. These values are model checks. They are not site measurements.

The room card at the bottom left of the model is removed. The room list still controls navigation. The small map stays on the right where screen space permits. The changed rooms were also checked in the local browser.

Version data is in `lib/layouts.ts`. The source data in `lib/plan.ts` and `public/measurements.json` describes version 1. The furniture dimensions file describes version 1, including its corrected corner cabinets. Run `npm run test:layouts` to check the added walls, island, retained structure, TV size, cabinet directions, furniture bounds, room access, area outlines, all three versions, and resource release. The tests use model geometry without a graphics context. They do not replace a physical device check or a site survey.

The interface is in Romanian. Select **Plimbare**, then the start control. Use W A S D or the arrow keys to move. Use the mouse to look. Hold Shift to move faster. Press Esc to release the mouse. On a touch screen, drag the scene to look and use the four arrow controls to move. The model has no room labels or dimension lines. During movement, furniture within 20 cm of a fixed boundary does not block the player. Free-standing furniture remains solid. Walls, columns, shafts, glazing, and the island remain solid. Hiding furniture removes its movement obstacles. Run `npm run test:navigation` to check these rules.

On iPad, use **Încăperi** to open the room list. The model uses the available screen in portrait, landscape, and Split View. In the 3D view, drag one finger to rotate. Use two fingers to zoom and move the view. In the plan, drag one finger to move the view. In **Plimbare**, press **Începe plimbarea**, hold a direction arrow with one hand, and drag the scene with the other hand to look. **Pauză** stops movement. The full-screen control also has an expanded layout when the browser cannot enter native full screen.

The touch renderer limits pixel density to reduce graphics work. Static shadows update when the walls or furniture change. The model can restore its graphics context after a temporary loss. Safe-area padding keeps controls clear of the screen edges.

The plan data is in `lib/plan.ts`. Architecture and furniture are in `lib/build-home.ts`. Camera and input controls are in `lib/home-scene.ts`. Collision rules are in `lib/navigation.ts`.

The apartment PDF labels the apartments 11 and 12. The A07 full-floor PDF labels the same layouts 6 and 7. The west apartment is 6/11. The east apartment is 7/12. See `MEASUREMENT_AUDIT.md` for the three measurement checks and `public/measurements.json` for the source values and differences.

The new hall opening is 1.20 m wide and 2.15 m high. The guest room has a proposed south partition and an east door near the opening in A07. The apartment PDF shows a different guest opening. The model keeps the concrete columns and service shafts. The new hall opening is a design proposal. A structural engineer must check the wall before site work.

Wall height is an assumed 2.70 m. The source mark +6.40 m is the floor elevation. It is not the room height. The child's room uses the A07 dimension of 3.00 m. The apartment PDF gives 3.05 m. This difference remains unresolved. Internal partitions follow the 0.125 m vector width, rounded to 13 cm in the labels. Exterior walls include 0.25 m wall and 0.15 m insulation. Party and stair-entry walls are 0.25 m. Room areas are source labels. Bounding rectangles must not be summed. The small map uses outlines at recesses and shafts.

Furniture uses custom geometry and local material textures. In versions 1 and 2, the east room has a large kitchen, a 1.90 × 0.90 m dining table for six, a 2.10 × 1.65 m corner sofa and a 0.50 × 0.50 m coffee table, and tall cabinets for the fridge, ovens, and pantry. In version 1, the west room has a sage sofa, two cream armchairs, an oval wood coffee table, and an office desk with storage. In version 1, the guest room has the second desk. The living sofa ends at the east edge of its solid rear wall. The lamp is removed. One armchair is moved to the side to clear the TV cabinet. The library beside the windows is removed. The hall to the dining room has a 1.80 × 0.42 m coat wardrobe with sliding fronts. The small hall beside the bathroom has a second wardrobe, 1.00 m wide and 0.60 m deep, with sliding doors. The living room has a TV and low cabinet against the south wall. In versions 1 and 2, the dining table is turned 90° beside the east window. The sofa has its back towards the kitchen. The brown cabinet beside the sofa and its back panel, vase, and mirror are removed. A new TV on the south wall has an active screen diagonal of 1.80 m. The version 1 child room has a wood house bed, cloud lights, closed toy and book storage, and a small reading tent. Version 1 has three bedrooms, two bathrooms, and a utility room with WC. Both balconies have teak storage benches, planted wall panels, and stone planters. The third-person camera can be added to the existing camera modes later.

The two north bathrooms follow the fixture arrangement in A07. The west WC faces south. The east WC faces east. The west bathroom corner is closed. Furniture and fixture sizes are design choices unless the source gives a size. The main wardrobe and guest desk leave the balcony openings clear. In versions 1 and 2, the dining table moves 20 cm towards the east window. The sofa, its rug, and its coffee table move 40 cm in the same direction. The dining clearance checks use these positions. All six chairs can move 45 cm away from the table. The end chairs and the two chairs beside the window sit partly below the table when stored. The coat wardrobe leaves 0.86 m clear within the hall. The small hall wardrobe leaves a 1.33 m passage and clears both room doors. See the measurement report for changed furniture sizes.

Run `npm run check` for the type check. Run `npm run test:model` for 18 wall-face checks, column and shaft checks, furniture mesh checks, and movement checks. The movement test uses a 0.08 m grid and a player radius of 0.19 m. It checks each room, all four balcony openings, kitchen access, and desk access from the entry. It also checks the corrected doors, bathroom wall, WC positions, and number of desks and dining chairs. Run `npm run build:pages` for the published files.

Automated browser image and interaction checks were not run. The local preview returned HTTP 200. Full-project lint has existing errors in the shared UI and viewer code; the changed model and verification files pass lint. WebMCP registration is optional. No supported WebMCP test context was available in this session.

Run `npm run test:touch` to check multiple fingers, input cancellation, pause, keyboard input, touch view modes, and portrait framing. These are code checks. A physical iPad was not available for device tests.

The three reference photos guide the room colours, materials, and furniture style. The botanical wall prints use a generated image at `public/artwork/botanical-pair.jpg`. The original reference photos are not website assets.
